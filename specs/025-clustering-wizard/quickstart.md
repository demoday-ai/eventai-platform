# Quickstart Verification: Clustering Wizard

1. Open /clustering page
2. Verify empty state shows when no projects loaded
3. Set room count = 3, click "Запустить"
4. Wait for clustering to complete — verify rooms with projects appear
5. Navigate to step 2 (Результат) — verify rooms grid with names, descriptions, project counts
6. Navigate to step 3 (Перемещение) — move a project to another room — verify counts update
7. Navigate to step 4 (Одобрение) — verify summary shows
8. Click "Одобрить" — verify confirmation dialog appears
9. Confirm — verify "Кластеризация одобрена" message + next-step hint with link to /experts
10. Verify Global Stepper updates clustering step status
